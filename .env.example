# Auto-Grader Environment Variables
# Copy this to .env.local and fill in your values

# MongoDB Atlas Connection String
# Get from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/auto-grader

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scope: public_repo (for classic) or Contents read-only (for fine-grained)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# App Password Protection (required)
# Set these in .env.local (never commit real values)
APP_PASSWORD=change-me

# Used to sign the 24-hour auth cookie (required)
# Generate a long random string (at least 32 chars)
AUTH_SECRET=change-me-to-a-long-random-string

# Email (Gmail SMTP - App Password)
# 1) Enable 2FA on the Gmail account
# 2) Create an App Password and use it as SMTP_PASS
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-gmail-address@gmail.com
SMTP_PASS=your-app-password

# Sender identity
EMAIL_FROM_NAME=Instructor Adewale
# Defaults to SMTP_USER if not set
EMAIL_FROM_ADDRESS=your-gmail-address@gmail.com

# Cron secret for processing queued emails (used by /api/cron/process-emails)
CRON_SECRET=change-me-to-a-long-random-string